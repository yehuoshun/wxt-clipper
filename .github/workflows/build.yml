name: Build & Release

on:
  push:
    branches: [main]
    tags: ['v*']

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - run: npm ci

      - name: Build Chrome
        run: npx wxt build -b chrome

      - name: Build Firefox
        run: npx wxt build -b firefox

      - name: Zip Chrome
        run: cd .output/chrome-mv3 && zip -r ../../wxt-clipper-chrome.zip .

      - name: Zip Firefox
        run: cd .output/firefox-mv2 && zip -r ../../wxt-clipper-firefox.zip .

      - name: Upload Chrome artifact
        uses: actions/upload-artifact@v4
        with:
          name: wxt-clipper-chrome
          path: wxt-clipper-chrome.zip

      - name: Upload Firefox artifact
        uses: actions/upload-artifact@v4
        with:
          name: wxt-clipper-firefox
          path: wxt-clipper-firefox.zip

      - name: Create Release (tag only)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            wxt-clipper-chrome.zip
            wxt-clipper-firefox.zip
          generate_release_notes: true
